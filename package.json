{
  "name": "edupulse",
  "version": "1.0.0",
  "description": "EduPulse - MERN E-Learning Platform with Performance Tracking",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../client && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm start",
    "seed": "cd backend && npm run seed",
    "start": "concurrently \"npm run dev:backend\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
